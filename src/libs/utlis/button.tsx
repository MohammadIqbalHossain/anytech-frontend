import { inter } from "@/app/ui/fonts";
import { FC, ReactNode } from "react";

interface IButton {
  children: ReactNode;
  classname: string;
}

const Button: FC = () => {
  return <button></button>;
};
